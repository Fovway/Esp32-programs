# Прошивка №1: Часы для ESP32 + OLED 0.96" (SSD1306 128x64)

Эта прошивка показывает:
- текущее время (крупно, по центру),
- текущую дату (ниже времени),
- статус Wi‑Fi и синхронизации NTP в верхней строке.

При старте устройство:
1. подключается к Wi‑Fi,
2. синхронизирует время через NTP,
3. отображает время и дату на OLED.

## Подключение дисплея (I2C)

Для типового модуля SSD1306:
- `GND` -> `GND` ESP32
- `VCC` -> `3.3V` ESP32
- `SCL` -> `GPIO 22` (по умолчанию у большинства плат ESP32)
- `SDA` -> `GPIO 21` (по умолчанию у большинства плат ESP32)

> Если у вашей платы другие I2C-пины, добавьте в `setup()`:
> `Wire.begin(SDA_PIN, SCL_PIN);`

## Что нужно установить в Arduino IDE

1. Плату **ESP32 by Espressif Systems**.
2. Библиотеки:
   - `Adafruit GFX Library`
   - `Adafruit SSD1306`

(`WiFi.h` и `time.h` входят в пакет ESP32.)

## Настройка

Откройте файл `clock_oled.ino` и укажите Wi‑Fi:

```cpp
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
```

По умолчанию выставлен часовой пояс **UTC+3 (Москва)**.

Если нужен другой пояс — измените:

```cpp
constexpr long GMT_OFFSET_SECONDS = 3 * 3600;
constexpr int DAYLIGHT_OFFSET_SECONDS = 0;
```

## Идея по внешнему виду

Интерфейс сделан «чистым» и читаемым для 128x64:
- сверху компактная статусная строка,
- в центре крупные часы,
- внизу дата,
- мигающее двоеточие добавляет эффект «живых» часов.

## Дальше можно улучшить

- добавить выбор 12/24-часового формата,
- показывать день недели,
- добавить русские названия месяцев (через кастомный шрифт),
- добавить веб-страницу настройки Wi‑Fi (Captive Portal).
